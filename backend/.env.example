# Failure Academy backend configuration
# Replace placeholder values with real credentials before running in production.

# Required: used for JWT signing. Generate a long random string.
JWT_SECRET_KEY=change_this_super_secret_key

# Optional but recommended: override default frontend origin for CORS.
FRONTEND_ORIGIN=http://localhost:5173

# SQLite connection string or external database URL.
DATABASE_URL=sqlite:///./failure_academy.db

# YouTube Data API key from Google Cloud Console.
YOUTUBE_API_KEY=
# Update the channel handle if you use a different YouTube channel.
YOUTUBE_CHANNEL_HANDLE=@academyfailure

# Telegram bot integration. Add your bot to the group and capture the chat ID via /api/telegram/updates.
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
